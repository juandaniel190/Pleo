version: 2

models:
  - name: fct_opportunities
    description: Opportunity-grain fact with is_stale, weighted ARR, and closes_in_q2_2026.
    columns:
      - name: opportunity_id
        data_tests: [unique, not_null]
